import { HOME } from '@/content/data/home'

/**
 * Vídeos del hero de la home. El primero es el del cliente (`/media/hero.mp4`):
 * H.264, sin audio y con el primer fotograma como póster para que no haya salto
 * al arrancar. Con más de uno, `<HeroVideo>` los alterna.
 */
export const HERO_CLIPS = HOME.hero.videos
