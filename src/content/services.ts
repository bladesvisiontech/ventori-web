import { SERVICIOS } from '@/content/data/servicios'

export const SERVICES = SERVICIOS.services

/** Encabezado de la lista de entregables en el detalle de cada servicio. */
export const SERVICE_DETAIL = SERVICIOS.detail
