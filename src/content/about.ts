import { GLOBAL } from '@/content/data/global'
import { HOME } from '@/content/data/home'
import { NOSOTROS } from '@/content/data/nosotros'

/** Textos institucionales (página Nosotros). Editables desde el CMS. */
export const ABOUT = {
  eyebrow: NOSOTROS.about.eyebrow,
  heading: NOSOTROS.about.heading,
  paragraphs: NOSOTROS.about.paragraphs,
  image: NOSOTROS.about.image,
}

export const MISSION = NOSOTROS.mission
export const VISION = NOSOTROS.vision
export const COMMITMENT = NOSOTROS.commitment

/** Copy del hero de la home. */
export const HERO = HOME.hero

/** Banda de cierre, reutilizada en varias páginas. */
export const CTA_BAND = GLOBAL.ctaBand
